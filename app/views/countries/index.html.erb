
<h1>All Countries </h1>


<% @countries.each do |country| %>
	<h2> <%= link_to country.name, country_path(country), methos: :get %></h2>
<% end %>

<%= link_to "New Country", new_country_path, method: :get %>

